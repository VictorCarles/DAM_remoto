//

function calcularArea(base, altura){
    let area;
    area = base * altura;
    console.log ("El area es", area);
}

// Programa principal

let b = 30, a = 23;
calcularArea(b,a);
calcularArea(56, 6+8);